# Backend Environment Variables
# Copy this file to .env and fill in the actual values

# ==================================
# Node Environment
# ==================================
NODE_ENV=development
# Set to 'production' when deployed to Render

# ==================================
# Port Configuration
# ==================================
PORT=5000
# Render will set this automatically to 0 (any available port)

# ==================================
# Database Configuration - LOCAL DEVELOPMENT (Docker/Local)
# ==================================
# Use these when running locally with docker-compose
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=todo_app

# ==================================
# Database Configuration - PRODUCTION (Render)
# ==================================
# When deploying to Render, set DATABASE_URL instead:
# DATABASE_URL=mysql://username:password@host:port/database
# This will be provided by Render's MySQL service
# DATABASE_URL=

# ==================================
# JWT Configuration
# ==================================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRY=24h

# ==================================
# CORS Configuration (Production)
# ==================================
# For Render deployment, specify your frontend URL:
# FRONTEND_URL=https://your-frontend.onrender.com
FRONTEND_URL=

# ==================================
# Notes for Deployment
# ==================================
# 1. When using Render:
#    - Render MySQL will provide DATABASE_URL automatically in the environment
#    - Do NOT set DB_HOST, DB_USER, DB_PASSWORD, DB_NAME in Render
#    - Only set JWT_SECRET and FRONTEND_URL manually in Render dashboard
#
# 2. For local development with Docker:
#    - Use DB_HOST, DB_USER, DB_PASSWORD, DB_NAME
#    - Do NOT set DATABASE_URL
#    - These are used in docker-compose.yml
